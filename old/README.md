# Legacy archive (frozen)

This folder is a **dead archive** of the pre-rebuild UI and Express routers.

- **Not mounted** by Next.js or Express
- **Not imported** by the live app
- Do **not** add features here

Live code:
- UI: `app/`, `components/`
- API: `server/modules/`
