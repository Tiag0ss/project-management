# Root package only (not a multi-package monorepo).
# Required so older tooling that still expects a packages list does not fail;
# pnpm 10+ also stores install/build settings here.
packages:
  - '.'

allowBuilds:
  '@scarf/scarf': true
  bcrypt: true
  electron: true
  sharp: true
  unrs-resolver: true
